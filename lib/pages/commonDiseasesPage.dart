import 'package:flutter/material.dart';

class CommonDiseases extends StatelessWidget {
  const CommonDiseases({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
    // TODO: MAKE PAGE
  }
}
